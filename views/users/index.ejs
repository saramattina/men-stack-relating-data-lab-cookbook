<!DOCTYPE html>
<html lang="en">
<head>
   <meta charset="UTF-8">
   <meta name="viewport" content="width=device-width, initial-scale=1.0">
   <title>View Other Pantries</title>
</head>
<body>
   <%- include('../partials/_navbar.ejs') %>
   <h1>Check Out Other Pantries!</h1>

   <ul>
      <% users.forEach((user) => { %>
        <a href="/users/<%= user._id %>">
          <li><%= user.username %></li>
        </a>
      <% }) %>
    </ul>

</body>
</html>